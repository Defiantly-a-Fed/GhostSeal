# Ghost Seal repo patch package

This package adds visible Ghost Seal working material to the fork:

- `node1_bridge/ghostseal_bridge.py` — Node 1 serial bridge code
- `node1_bridge/requirements.txt` — Python dependencies
- `node1_bridge/scripts/install_node1_bridge.sh` — Pi-side installer
- `node1_bridge/systemd/ghostseal-bridge-status.service` — service example
- `GHOSTSEAL_STATUS.md` — current repo/code status
- `GHOSTSEAL_COMMAND_PARITY.md` — command parity tracking

Commit message:

```text
Add Ghost Seal Node 1 bridge code
```

Commit description:

```text
Adds the working Node 1 serial bridge, installation files, status documentation, and command parity tracking so Ghost Seal code is visible in the repository.
```
